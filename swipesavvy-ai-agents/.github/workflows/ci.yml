name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Set up Python
      uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest-xdist  # For parallel testing
    
    - name: Code formatting check (Black)
      run: |
        black --check services/ tools/ tests/
    
    - name: Import sorting check (isort)
      run: |
        isort --check-only services/ tools/ tests/
    
    - name: Lint (Flake8)
      run: |
        flake8 services/ tools/ tests/ --max-line-length=100 --extend-ignore=E203,W503
    
    - name: Type checking (MyPy)
      run: |
        mypy services/ --ignore-missing-imports
      continue-on-error: true  # Don't fail build on type errors initially
    
    - name: Run unit tests
      run: |
        pytest tests/unit/ -v --cov=services --cov=tools --cov-report=xml --cov-report=term
    
    - name: Run integration tests
      run: |
        pytest tests/integration/ -v
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        REDIS_URL: redis://localhost:6379/1
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@18283e04ce6e62d37312384ff67231eb8fd56d24 # v5.4.0
      with:
        file: ./coverage.xml
        fail_ci_if_error: false

  security-scan:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Run Bandit security scan
      run: |
        pip install bandit
        bandit -r services/ tools/ -f json -o bandit-report.json

    - name: Run Safety check for dependencies
      run: |
        pip install safety
        safety check --json

  build-docker:
    runs-on: ubuntu-latest
    needs: [lint-and-test]
    if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Build Docker images
      run: |
        echo "Docker build step - to be implemented in Week 4"
        # docker build -t swipesavvy-concierge:${{ github.sha }} services/concierge-agent/
