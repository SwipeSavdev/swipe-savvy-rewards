# Production Environment Configuration
ENVIRONMENT=production
NODE_ENV=production
PORT=8000
DEBUG=false

# Security - JWT Configuration (REQUIRED - Set in CI/CD System)
JWT_SECRET_KEY=${JWT_SECRET_KEY}
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Demo Users MUST Be Disabled in Production
DEMO_USERS={}

# API Keys (Set in CI/CD System - NEVER in source code)
TOGETHER_API_KEY=${TOGETHER_API_KEY}
OPENAI_API_KEY=${OPENAI_API_KEY}

# Database (Set in CI/CD System - NEVER in source code)
DATABASE_URL=${DATABASE_URL}

# Rate Limiting - More aggressive in production
RATE_LIMIT_ENABLED=true
RATE_LIMIT_CHAT=30/minute
RATE_LIMIT_AUTH=2/minute
RATE_LIMIT_USER_CREATION=3/minute
