# Dependencies
node_modules/
.pnp
.pnp.js

# Environment variables (NEVER commit actual env files — PCI DSS 2.2.2)
.env
.env.local
.env.development
.env.staging
.env.production
.env.development.local
.env.test.local
.env.production.local
.env.staging.local
.env.*.local

# Secrets (DO NOT COMMIT)
*.key
*.pem
secrets/
.secrets

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Documentation archive (work session docs)
docs/archive/

# Build outputs
dist/
build/
.next/
out/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# OS files
.DS_Store
Thumbs.db

# Expo
.expo/
*.jks
*.p8
*.p12
*.mobileprovision

# Deploy scripts (keep local)
deploy-*.sh

# Terraform
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform/
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Append these lines to the target repo's .gitignore.
# If the repo has no .gitignore yet, copy this file's contents into a new .gitignore.

# Claude Code internals — local-only files that MUST NOT be committed.
# - `settings.local.json` is per-developer permission overrides (Anthropic convention).
# - `CLAUDE.local.md` is project-specific notes for one dev only.
# - `memory/` contains auto-memory — personal to each dev.
# - `scheduled_tasks*.lock` is runtime scheduler state.
.claude/scheduled_tasks*.lock
.claude/settings.local.json
.claude/memory/
CLAUDE.local.md

# macOS metadata
.DS_Store

# Keys & credentials
keys/
**/keys/
credentials.json
**/credentials.json
*.p8
*.pem
*.key
*.jks
*.p12

# Terraform variables (often contain secrets)
*.tfvars
!*.tfvars.example
terraform.tfvars.local

# local virtualenvs — never commit
.venv*/
**/.venv*/
