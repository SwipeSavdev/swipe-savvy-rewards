# 🧩 Test Scaffolding (k6 + Playwright + Jest/JUnit)

This folder provides *starter harnesses* aligned to the master prompts.
You should copy the templates into the relevant repos and customize endpoints, auth, and data factories.

Folders:
- `k6/` – load test harness with concurrency + correctness checks
- `playwright/` – E2E harness (web/admin/merchant flows)
- `jest/` – JS/TS unit test starter kit
- `junit/` – Java unit/integration test starter kit
